import { View } from "native-base";
import { useSelector } from "react-redux";

function Subscribers() {

    const list = useSelector(get)

    return (<View></View>  );
}

export default Subscribers;